# Set this to the Render URL after the backend is deployed.
# The backend must also list the Vercel origin in VERISHIELD_CORS_ORIGINS --
# in production there is no dev proxy to hide behind.
REACT_APP_API_URL=https://your-backend.onrender.com
