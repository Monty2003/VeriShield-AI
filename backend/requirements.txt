# Core API + validation. Installs in ~1 minute and is enough to run the
# pipeline end-to-end with rule-based stages. Heavy ML models live in
# requirements-ml.txt so the app is always runnable during development.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

# Imaging: needed for ingest, region cropping and classical forensics.
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==11.0.0
# HEIC/HEIF. Modern phones shoot this by default; without it the service
# cannot read the native format of most cameras its users hold.
pillow-heif==0.21.0

# Datastores (optional at runtime -- app degrades gracefully if absent)
# pymongo is pinned transitively by motor -- do not pin it here, the ranges conflict
motor==3.6.0
redis==5.2.1
qdrant-client==1.12.2
minio==7.2.13

# X.509 parsing and RSA signature verification for the Aadhaar Secure QR.
# A vetted library rather than hand-rolled PKCS#1 checks: lax padding
# parsing in home-made verifiers is exactly how signature forgeries happen.
cryptography==45.0.7

# Reading the Aadhaar Secure QR. zxing-cpp is the decoder: self-contained
# wheels, and on real phone photographs it reads 5 of 7 card backs where zbar
# reads 2. pyzbar is an optional fallback, used only when its zbar shared
# library is present (bundled on Windows, `libzbar0` on Debian/Ubuntu).
zxing-cpp==3.1.1
pyzbar==0.1.9

python-dateutil==2.9.0.post0
rapidfuzz==3.11.0

pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
# Redis semantics in tests without a Redis server -- including two stores on
# one server, which is how the multi-worker behaviour is tested.
fakeredis==2.38.0
# MongoDB semantics in tests, so no test ever writes to the real audit trail.
mongomock==4.3.0
